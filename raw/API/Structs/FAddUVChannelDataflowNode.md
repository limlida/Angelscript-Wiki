### FAddUVChannelDataflowNode


* Add a new UV channel to the collection
* note that there's a maximum of 8 channels that can be handled by a collection

**属性**:

- `FVector2f DefaultValue [Value to initialize the UV with]`
- `FLinearColor OverrideColor []`
- `bool bActive [Used to calculate ContainerElementOffsets]`
- `bool bOverrideColor []`


**方法**:

- `FAddUVChannelDataflowNode& opAssign(FAddUVChannelDataflowNode Other)`

---

