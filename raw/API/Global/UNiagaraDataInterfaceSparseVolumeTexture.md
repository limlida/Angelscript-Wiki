### UNiagaraDataInterfaceSparseVolumeTexture


Data Interface allowing sampling of a sparse volume texture

**属性**:

- `bool BlockingStreamingRequests []`
- `USparseVolumeTexture SparseVolumeTexture []`
- `FNiagaraUserParameterBinding SparseVolumeTextureUserParameter [When valid the user parameter is used as the texture rather than the one on the data interface]`

---

