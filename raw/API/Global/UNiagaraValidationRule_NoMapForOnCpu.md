### UNiagaraValidationRule_NoMapForOnCpu


This validation rule checks that map for nodes are not used with cpu scripts (as they only work on gpu).

**属性**:

- `ENiagaraValidationSeverity Severity [How do we want to report the error in the stack]`

---

