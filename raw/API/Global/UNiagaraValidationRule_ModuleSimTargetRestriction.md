### UNiagaraValidationRule_ModuleSimTargetRestriction


This validation rule checks that a module is only used in emitters with the configured runtime target.

**属性**:

- `ENiagaraValidationSeverity Severity [How do we want to report the error in the stack]`
- `ENiagaraSimTarget SupportedSimTarget []`

---

