### UVirtualTextureCollection


**属性**:

- `bool bAllowFormatConversion [Allow format conversions, including differing compression schemes.
This has a potentially large runtime overhead.]`
- `bool bIsSRGB [If this texture collection if in SRGB, requires format conversion.
Textures not matching this will be converted at runtime.]`

---

