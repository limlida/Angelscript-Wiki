### UNarrativeImpactInterface


**方法**:

- `HandleExplosionImpact(UNarrativeAbilitySystemComponent ExplosionCauser, FVector ExplosionLocation, float IntendedDamage)`  
  Handle what should happen when a vehicle collides with us.
- `HandleVehicleImpact(ANarrativeVehicleBase Vehicle, UPrimitiveComponent OverlappedComponent, UPrimitiveComponent OtherComp, int OtherBodyIndex, bool bFromSweep, FHitResult SweepResult)`  
  Handle what should happen when a vehicle collides with us.

---

