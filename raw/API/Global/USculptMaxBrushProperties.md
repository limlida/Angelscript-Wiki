### USculptMaxBrushProperties


**属性**:

- `float32 MaxHeight [Specify maximum displacement height (relative to brush size)]`
- `bool bFreezeCurrentHeight [Use maximum height from last brush stroke, regardless of brush size. Note that spatial brush falloff still applies.]`

---

