### UPoseWatchPoseElement


**属性**:

- `float32 BlendScaleThreshold [The threshold which each bone's blend scale much surpass to be rendered using the viewport mask]`
- `UBlendProfile ViewportMask [Optionally select a Blend Mask to control which bones on the skeleton are rendered. Any non-zero entries are rendered.]`
- `FVector3d ViewportOffset [Offset the rendering of the bones in the viewport.]`
- `bool bInvertViewportMask [Invert which bones are rendered when using a viewport mask]`

---

