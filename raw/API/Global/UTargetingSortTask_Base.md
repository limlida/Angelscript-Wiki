### UTargetingSortTask_Base


@class TargetingSortTask_Base

A base class that has the basic setup for a sort task.

**属性**:

- `bool bAscending`
- `bool bStableSort [Should this task use a (slightly slower) sorting algorithm that preserves the relative ordering of targets with equal scores?]`

---

