### UNetworkSettings


Network settings.

**属性**:

- `bool bEnableMultiplayerWorldOriginRebasing [If true, origin rebasing is enabled in multiplayer games, meaning that servers and clients can have different local world origins.]`
- `bool bVerifyPeer [If true, libcurl authenticates the peer's certificate. Disable to allow self-signed certificates.]`

---

