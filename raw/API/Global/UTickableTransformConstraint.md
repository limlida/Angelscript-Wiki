### UTickableTransformConstraint


UTickableTransformConstraint

**属性**:

- `UTransformableHandle ChildTRSHandle [The transformable handle representing the child of that constraint.]`
- `UTransformableHandle ParentTRSHandle [The transformable handle representing the parent of that constraint.]`
- `bool bDynamicOffset [Should the child be able to change it's offset dynamically.]`
- `bool bMaintainOffset [Should that constraint maintain the default offset.]`
- `bool bUseCurrentOffset [Used at creation to define whether the current relative offset should be used instead of the provided one.]`

---

