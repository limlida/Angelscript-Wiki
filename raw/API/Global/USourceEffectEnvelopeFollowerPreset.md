### USourceEffectEnvelopeFollowerPreset


**属性**:

- `FSourceEffectEnvelopeFollowerSettings Settings`


**方法**:

- `RegisterEnvelopeFollowerListener(UEnvelopeFollowerListener EnvelopeFollowerListener)`  
  Registers an envelope follower listener with the effect.
- `SetSettings(FSourceEffectEnvelopeFollowerSettings InSettings)`
- `UnregisterEnvelopeFollowerListener(UEnvelopeFollowerListener EnvelopeFollowerListener)`  
  Unregisters an envelope follower listener with the effect.

---

